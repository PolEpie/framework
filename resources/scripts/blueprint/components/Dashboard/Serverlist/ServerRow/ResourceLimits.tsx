import React from 'react';
import { Server } from '@/api/server/getServer';
/* blueprint/import */

export default ({arg} : {arg: Server}) => {
  return (
    <>
      {/* blueprint/react */}
    </>
  );
};
